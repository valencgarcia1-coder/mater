# Python
__pycache__/
*.pyc
.venv/
*.egg-info/
detector/.toolchain/

# Detector outputs — never commit video, snapshots, or model weights
*.mp4
*.avi
*.mov
*.mkv
# ...except the landing page's own hero video, which is a real site asset
!dashboard/public/hero.mp4
detector/*.pt
detector/eval/clips/
detector/eval/results/
detector/spaces.yaml
detector/parking_timers.json
detector/calibration.json
detector/events.jsonl

# Secrets
.env
*.env

# OS
.DS_Store
